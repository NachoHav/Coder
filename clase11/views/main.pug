extends layout.pug

block content
    div(class='d-flex flex-column justify-content-center align-items-center')
        h1(class='text-center mb-4 text-light') API de Productos
        div
            a(href='/api/productos/listar' class='btn btn-danger me-2') LISTADO DE PRODUCTOS
            a(href='/api/productos/cargar' class='btn btn-success') AGREGAR PRODUCTO